# Frontend (Vite — client-side, prefix with VITE_)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: override API endpoint (defaults to /api/analyze-feedback)
# VITE_API_URL=/api/analyze-feedback

# Backend / Vercel Serverless Function (server-side only)
GEMINI_API_KEY=your-gemini-api-key
